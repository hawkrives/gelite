name: Package Build Dry Run

on:
  workflow_dispatch:
    inputs:
      gelpkg_ref:
        description: "gel-pkg git ref used to build the packages"
        default: "master"
      metapkg_ref:
        description: "metapkg git ref used to build the packages"
        default: "master"

jobs:
  prep:
    runs-on: ubuntu-latest

    outputs:

      if_debian_buster_x86_64: ${{ steps.scm.outputs.if_debian_buster_x86_64 }}

      if_debian_buster_aarch64: ${{ steps.scm.outputs.if_debian_buster_aarch64 }}

      if_debian_bullseye_x86_64: ${{ steps.scm.outputs.if_debian_bullseye_x86_64 }}

      if_debian_bullseye_aarch64: ${{ steps.scm.outputs.if_debian_bullseye_aarch64 }}

      if_debian_bookworm_x86_64: ${{ steps.scm.outputs.if_debian_bookworm_x86_64 }}

      if_debian_bookworm_aarch64: ${{ steps.scm.outputs.if_debian_bookworm_aarch64 }}

      if_ubuntu_focal_x86_64: ${{ steps.scm.outputs.if_ubuntu_focal_x86_64 }}

      if_ubuntu_focal_aarch64: ${{ steps.scm.outputs.if_ubuntu_focal_aarch64 }}

      if_ubuntu_jammy_x86_64: ${{ steps.scm.outputs.if_ubuntu_jammy_x86_64 }}

      if_ubuntu_jammy_aarch64: ${{ steps.scm.outputs.if_ubuntu_jammy_aarch64 }}

      if_ubuntu_noble_x86_64: ${{ steps.scm.outputs.if_ubuntu_noble_x86_64 }}

      if_ubuntu_noble_aarch64: ${{ steps.scm.outputs.if_ubuntu_noble_aarch64 }}

      if_centos_8_x86_64: ${{ steps.scm.outputs.if_centos_8_x86_64 }}

      if_centos_8_aarch64: ${{ steps.scm.outputs.if_centos_8_aarch64 }}

      if_rockylinux_9_x86_64: ${{ steps.scm.outputs.if_rockylinux_9_x86_64 }}

      if_rockylinux_9_aarch64: ${{ steps.scm.outputs.if_rockylinux_9_aarch64 }}

      if_linux_x86_64: ${{ steps.scm.outputs.if_linux_x86_64 }}

      if_linux_aarch64: ${{ steps.scm.outputs.if_linux_aarch64 }}

      if_linuxmusl_x86_64: ${{ steps.scm.outputs.if_linuxmusl_x86_64 }}

      if_linuxmusl_aarch64: ${{ steps.scm.outputs.if_linuxmusl_aarch64 }}

      if_macos_x86_64: ${{ steps.scm.outputs.if_macos_x86_64 }}

      if_macos_aarch64: ${{ steps.scm.outputs.if_macos_aarch64 }}


    steps:
    - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1


    - name: Determine SCM revision
      id: scm
      shell: bash
      run: |
        rev=$(git rev-parse HEAD)
        jq_filter='.packages[] | select(.basename == "gel-server") | select(.architecture == $ARCH) | .version_details.metadata.scm_revision | . as $rev | select(($rev != null) and ($REV | startswith($rev)))'

        key="debian-buster-x86_64"
        val=true


        idx_file=buster.nightly.json
        url=https://packages.edgedb.com/apt/.jsonindexes/$idx_file


        tmp_file="/tmp/$idx_file"

        if [ ! -e "$tmp_file" ]; then
          curl --fail -o $tmp_file -s $url || true
        fi
        if [ -e "$tmp_file" ]; then
          out=$(< "$tmp_file" jq -r --arg REV "$rev" --arg ARCH "x86_64" "$jq_filter")
          if [ -n "$out" ]; then
            echo "Skip rebuilding existing ${key}"
            val=false
          fi
        fi

        echo if_${key//-/_}="$val" >> $GITHUB_OUTPUT


        key="debian-buster-aarch64"
        val=true


        idx_file=buster.nightly.json
        url=https://packages.edgedb.com/apt/.jsonindexes/$idx_file


        tmp_file="/tmp/$idx_file"

        if [ ! -e "$tmp_file" ]; then
          curl --fail -o $tmp_file -s $url || true
        fi
        if [ -e "$tmp_file" ]; then
          out=$(< "$tmp_file" jq -r --arg REV "$rev" --arg ARCH "aarch64" "$jq_filter")
          if [ -n "$out" ]; then
            echo "Skip rebuilding existing ${key}"
            val=false
          fi
        fi

        echo if_${key//-/_}="$val" >> $GITHUB_OUTPUT


        key="debian-bullseye-x86_64"
        val=true


        idx_file=bullseye.nightly.json
        url=https://packages.edgedb.com/apt/.jsonindexes/$idx_file


        tmp_file="/tmp/$idx_file"

        if [ ! -e "$tmp_file" ]; then
          curl --fail -o $tmp_file -s $url || true
        fi
        if [ -e "$tmp_file" ]; then
          out=$(< "$tmp_file" jq -r --arg REV "$rev" --arg ARCH "x86_64" "$jq_filter")
          if [ -n "$out" ]; then
            echo "Skip rebuilding existing ${key}"
            val=false
          fi
        fi

        echo if_${key//-/_}="$val" >> $GITHUB_OUTPUT


        key="debian-bullseye-aarch64"
        val=true


        idx_file=bullseye.nightly.json
        url=https://packages.edgedb.com/apt/.jsonindexes/$idx_file


        tmp_file="/tmp/$idx_file"

        if [ ! -e "$tmp_file" ]; then
          curl --fail -o $tmp_file -s $url || true
        fi
        if [ -e "$tmp_file" ]; then
          out=$(< "$tmp_file" jq -r --arg REV "$rev" --arg ARCH "aarch64" "$jq_filter")
          if [ -n "$out" ]; then
            echo "Skip rebuilding existing ${key}"
            val=false
          fi
        fi

        echo if_${key//-/_}="$val" >> $GITHUB_OUTPUT


        key="debian-bookworm-x86_64"
        val=true


        idx_file=bookworm.nightly.json
        url=https://packages.edgedb.com/apt/.jsonindexes/$idx_file


        tmp_file="/tmp/$idx_file"

        if [ ! -e "$tmp_file" ]; then
          curl --fail -o $tmp_file -s $url || true
        fi
        if [ -e "$tmp_file" ]; then
          out=$(< "$tmp_file" jq -r --arg REV "$rev" --arg ARCH "x86_64" "$jq_filter")
          if [ -n "$out" ]; then
            echo "Skip rebuilding existing ${key}"
            val=false
          fi
        fi

        echo if_${key//-/_}="$val" >> $GITHUB_OUTPUT


        key="debian-bookworm-aarch64"
        val=true


        idx_file=bookworm.nightly.json
        url=https://packages.edgedb.com/apt/.jsonindexes/$idx_file


        tmp_file="/tmp/$idx_file"

        if [ ! -e "$tmp_file" ]; then
          curl --fail -o $tmp_file -s $url || true
        fi
        if [ -e "$tmp_file" ]; then
          out=$(< "$tmp_file" jq -r --arg REV "$rev" --arg ARCH "aarch64" "$jq_filter")
          if [ -n "$out" ]; then
            echo "Skip rebuilding existing ${key}"
            val=false
          fi
        fi

        echo if_${key//-/_}="$val" >> $GITHUB_OUTPUT


        key="ubuntu-focal-x86_64"
        val=true


        idx_file=focal.nightly.json
        url=https://packages.edgedb.com/apt/.jsonindexes/$idx_file


        tmp_file="/tmp/$idx_file"

        if [ ! -e "$tmp_file" ]; then
          curl --fail -o $tmp_file -s $url || true
        fi
        if [ -e "$tmp_file" ]; then
          out=$(< "$tmp_file" jq -r --arg REV "$rev" --arg ARCH "x86_64" "$jq_filter")
          if [ -n "$out" ]; then
            echo "Skip rebuilding existing ${key}"
            val=false
          fi
        fi

        echo if_${key//-/_}="$val" >> $GITHUB_OUTPUT


        key="ubuntu-focal-aarch64"
        val=true


        idx_file=focal.nightly.json
        url=https://packages.edgedb.com/apt/.jsonindexes/$idx_file


        tmp_file="/tmp/$idx_file"

        if [ ! -e "$tmp_file" ]; then
          curl --fail -o $tmp_file -s $url || true
        fi
        if [ -e "$tmp_file" ]; then
          out=$(< "$tmp_file" jq -r --arg REV "$rev" --arg ARCH "aarch64" "$jq_filter")
          if [ -n "$out" ]; then
            echo "Skip rebuilding existing ${key}"
            val=false
          fi
        fi

        echo if_${key//-/_}="$val" >> $GITHUB_OUTPUT


        key="ubuntu-jammy-x86_64"
        val=true


        idx_file=jammy.nightly.json
        url=https://packages.edgedb.com/apt/.jsonindexes/$idx_file


        tmp_file="/tmp/$idx_file"

        if [ ! -e "$tmp_file" ]; then
          curl --fail -o $tmp_file -s $url || true
        fi
        if [ -e "$tmp_file" ]; then
          out=$(< "$tmp_file" jq -r --arg REV "$rev" --arg ARCH "x86_64" "$jq_filter")
          if [ -n "$out" ]; then
            echo "Skip rebuilding existing ${key}"
            val=false
          fi
        fi

        echo if_${key//-/_}="$val" >> $GITHUB_OUTPUT


        key="ubuntu-jammy-aarch64"
        val=true


        idx_file=jammy.nightly.json
        url=https://packages.edgedb.com/apt/.jsonindexes/$idx_file


        tmp_file="/tmp/$idx_file"

        if [ ! -e "$tmp_file" ]; then
          curl --fail -o $tmp_file -s $url || true
        fi
        if [ -e "$tmp_file" ]; then
          out=$(< "$tmp_file" jq -r --arg REV "$rev" --arg ARCH "aarch64" "$jq_filter")
          if [ -n "$out" ]; then
            echo "Skip rebuilding existing ${key}"
            val=false
          fi
        fi

        echo if_${key//-/_}="$val" >> $GITHUB_OUTPUT


        key="ubuntu-noble-x86_64"
        val=true


        idx_file=noble.nightly.json
        url=https://packages.edgedb.com/apt/.jsonindexes/$idx_file


        tmp_file="/tmp/$idx_file"

        if [ ! -e "$tmp_file" ]; then
          curl --fail -o $tmp_file -s $url || true
        fi
        if [ -e "$tmp_file" ]; then
          out=$(< "$tmp_file" jq -r --arg REV "$rev" --arg ARCH "x86_64" "$jq_filter")
          if [ -n "$out" ]; then
            echo "Skip rebuilding existing ${key}"
            val=false
          fi
        fi

        echo if_${key//-/_}="$val" >> $GITHUB_OUTPUT


        key="ubuntu-noble-aarch64"
        val=true


        idx_file=noble.nightly.json
        url=https://packages.edgedb.com/apt/.jsonindexes/$idx_file


        tmp_file="/tmp/$idx_file"

        if [ ! -e "$tmp_file" ]; then
          curl --fail -o $tmp_file -s $url || true
        fi
        if [ -e "$tmp_file" ]; then
          out=$(< "$tmp_file" jq -r --arg REV "$rev" --arg ARCH "aarch64" "$jq_filter")
          if [ -n "$out" ]; then
            echo "Skip rebuilding existing ${key}"
            val=false
          fi
        fi

        echo if_${key//-/_}="$val" >> $GITHUB_OUTPUT


        key="centos-8-x86_64"
        val=true


        idx_file=el8.nightly.json
        url=https://packages.edgedb.com/rpm/.jsonindexes/$idx_file


        tmp_file="/tmp/$idx_file"

        if [ ! -e "$tmp_file" ]; then
          curl --fail -o $tmp_file -s $url || true
        fi
        if [ -e "$tmp_file" ]; then
          out=$(< "$tmp_file" jq -r --arg REV "$rev" --arg ARCH "x86_64" "$jq_filter")
          if [ -n "$out" ]; then
            echo "Skip rebuilding existing ${key}"
            val=false
          fi
        fi

        echo if_${key//-/_}="$val" >> $GITHUB_OUTPUT


        key="centos-8-aarch64"
        val=true


        idx_file=el8.nightly.json
        url=https://packages.edgedb.com/rpm/.jsonindexes/$idx_file


        tmp_file="/tmp/$idx_file"

        if [ ! -e "$tmp_file" ]; then
          curl --fail -o $tmp_file -s $url || true
        fi
        if [ -e "$tmp_file" ]; then
          out=$(< "$tmp_file" jq -r --arg REV "$rev" --arg ARCH "aarch64" "$jq_filter")
          if [ -n "$out" ]; then
            echo "Skip rebuilding existing ${key}"
            val=false
          fi
        fi

        echo if_${key//-/_}="$val" >> $GITHUB_OUTPUT


        key="rockylinux-9-x86_64"
        val=true


        idx_file=el9.nightly.json
        url=https://packages.edgedb.com/rpm/.jsonindexes/$idx_file


        tmp_file="/tmp/$idx_file"

        if [ ! -e "$tmp_file" ]; then
          curl --fail -o $tmp_file -s $url || true
        fi
        if [ -e "$tmp_file" ]; then
          out=$(< "$tmp_file" jq -r --arg REV "$rev" --arg ARCH "x86_64" "$jq_filter")
          if [ -n "$out" ]; then
            echo "Skip rebuilding existing ${key}"
            val=false
          fi
        fi

        echo if_${key//-/_}="$val" >> $GITHUB_OUTPUT


        key="rockylinux-9-aarch64"
        val=true


        idx_file=el9.nightly.json
        url=https://packages.edgedb.com/rpm/.jsonindexes/$idx_file


        tmp_file="/tmp/$idx_file"

        if [ ! -e "$tmp_file" ]; then
          curl --fail -o $tmp_file -s $url || true
        fi
        if [ -e "$tmp_file" ]; then
          out=$(< "$tmp_file" jq -r --arg REV "$rev" --arg ARCH "aarch64" "$jq_filter")
          if [ -n "$out" ]; then
            echo "Skip rebuilding existing ${key}"
            val=false
          fi
        fi

        echo if_${key//-/_}="$val" >> $GITHUB_OUTPUT


        key="linux-x86_64"
        val=true


        idx_file=x86_64-unknown-linux-gnu.nightly.json
        url=https://packages.edgedb.com/archive/.jsonindexes/$idx_file


        tmp_file="/tmp/$idx_file"

        if [ ! -e "$tmp_file" ]; then
          curl --fail -o $tmp_file -s $url || true
        fi
        if [ -e "$tmp_file" ]; then
          out=$(< "$tmp_file" jq -r --arg REV "$rev" --arg ARCH "x86_64" "$jq_filter")
          if [ -n "$out" ]; then
            echo "Skip rebuilding existing ${key}"
            val=false
          fi
        fi

        echo if_${key//-/_}="$val" >> $GITHUB_OUTPUT


        key="linux-aarch64"
        val=true


        idx_file=aarch64-unknown-linux-gnu.nightly.json
        url=https://packages.edgedb.com/archive/.jsonindexes/$idx_file


        tmp_file="/tmp/$idx_file"

        if [ ! -e "$tmp_file" ]; then
          curl --fail -o $tmp_file -s $url || true
        fi
        if [ -e "$tmp_file" ]; then
          out=$(< "$tmp_file" jq -r --arg REV "$rev" --arg ARCH "aarch64" "$jq_filter")
          if [ -n "$out" ]; then
            echo "Skip rebuilding existing ${key}"
            val=false
          fi
        fi

        echo if_${key//-/_}="$val" >> $GITHUB_OUTPUT


        key="linuxmusl-x86_64"
        val=true


        idx_file=x86_64-unknown-linux-musl.nightly.json
        url=https://packages.edgedb.com/archive/.jsonindexes/$idx_file


        tmp_file="/tmp/$idx_file"

        if [ ! -e "$tmp_file" ]; then
          curl --fail -o $tmp_file -s $url || true
        fi
        if [ -e "$tmp_file" ]; then
          out=$(< "$tmp_file" jq -r --arg REV "$rev" --arg ARCH "x86_64" "$jq_filter")
          if [ -n "$out" ]; then
            echo "Skip rebuilding existing ${key}"
            val=false
          fi
        fi

        echo if_${key//-/_}="$val" >> $GITHUB_OUTPUT


        key="linuxmusl-aarch64"
        val=true


        idx_file=aarch64-unknown-linux-musl.nightly.json
        url=https://packages.edgedb.com/archive/.jsonindexes/$idx_file


        tmp_file="/tmp/$idx_file"

        if [ ! -e "$tmp_file" ]; then
          curl --fail -o $tmp_file -s $url || true
        fi
        if [ -e "$tmp_file" ]; then
          out=$(< "$tmp_file" jq -r --arg REV "$rev" --arg ARCH "aarch64" "$jq_filter")
          if [ -n "$out" ]; then
            echo "Skip rebuilding existing ${key}"
            val=false
          fi
        fi

        echo if_${key//-/_}="$val" >> $GITHUB_OUTPUT


        key="macos-x86_64"
        val=true


        idx_file=x86_64-unknown-linux-gnu.nightly.json
        url=https://packages.edgedb.com/archive/.jsonindexes/$idx_file


        tmp_file="/tmp/$idx_file"

        if [ ! -e "$tmp_file" ]; then
          curl --fail -o $tmp_file -s $url || true
        fi
        if [ -e "$tmp_file" ]; then
          out=$(< "$tmp_file" jq -r --arg REV "$rev" --arg ARCH "x86_64" "$jq_filter")
          if [ -n "$out" ]; then
            echo "Skip rebuilding existing ${key}"
            val=false
          fi
        fi

        echo if_${key//-/_}="$val" >> $GITHUB_OUTPUT


        key="macos-aarch64"
        val=true


        idx_file=aarch64-unknown-linux-gnu.nightly.json
        url=https://packages.edgedb.com/archive/.jsonindexes/$idx_file


        tmp_file="/tmp/$idx_file"

        if [ ! -e "$tmp_file" ]; then
          curl --fail -o $tmp_file -s $url || true
        fi
        if [ -e "$tmp_file" ]; then
          out=$(< "$tmp_file" jq -r --arg REV "$rev" --arg ARCH "aarch64" "$jq_filter")
          if [ -n "$out" ]; then
            echo "Skip rebuilding existing ${key}"
            val=false
          fi
        fi

        echo if_${key//-/_}="$val" >> $GITHUB_OUTPUT




  build-debian-buster-x86_64:
    runs-on: ['package-builder', 'self-hosted', 'linux', 'x64']
    needs: prep

    if: needs.prep.outputs.if_debian_buster_x86_64 == 'true'


    steps:
    - name: Build
      uses: docker://ghcr.io/geldata/gelpkg-build-debian-buster:latest@sha256:239b20ae6086fdef9d4a66284ba702346cdb16bab30f4b0b3534d47deab64b09
      env:
        PACKAGE: "edgedbpkg.edgedb:Gel"
        SRC_REF: "${{ github.sha }}"
        PKG_REVISION: "<current-date>"
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "debian"
        PKG_PLATFORM_VERSION: "buster"
        EXTRA_OPTIMIZATIONS: "true"
        METAPKG_GIT_CACHE: disabled
        GEL_PKG_REF: ${{ inputs.gelpkg_ref }}
        METAPKG_REF: ${{ inputs.metapkg_ref }}

    - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
      with:
        name: builds-debian-buster-x86_64
        path: artifacts/debian-buster

  build-debian-buster-aarch64:
    runs-on: ['package-builder', 'self-hosted', 'linux', 'arm64']
    needs: prep

    if: needs.prep.outputs.if_debian_buster_aarch64 == 'true'


    steps:
    - name: Build
      uses: docker://ghcr.io/geldata/gelpkg-build-debian-buster:latest@sha256:239b20ae6086fdef9d4a66284ba702346cdb16bab30f4b0b3534d47deab64b09
      env:
        PACKAGE: "edgedbpkg.edgedb:Gel"
        SRC_REF: "${{ github.sha }}"
        PKG_REVISION: "<current-date>"
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "debian"
        PKG_PLATFORM_VERSION: "buster"
        EXTRA_OPTIMIZATIONS: "true"
        METAPKG_GIT_CACHE: disabled
        GEL_PKG_REF: ${{ inputs.gelpkg_ref }}
        METAPKG_REF: ${{ inputs.metapkg_ref }}

    - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
      with:
        name: builds-debian-buster-aarch64
        path: artifacts/debian-buster

  build-debian-bullseye-x86_64:
    runs-on: ['package-builder', 'self-hosted', 'linux', 'x64']
    needs: prep

    if: needs.prep.outputs.if_debian_bullseye_x86_64 == 'true'


    steps:
    - name: Build
      uses: docker://ghcr.io/geldata/gelpkg-build-debian-bullseye:latest@sha256:c9013ed1c6a5347b4c20f240fcaaf3834e88949e46d05f40f496b879f82f8f95
      env:
        PACKAGE: "edgedbpkg.edgedb:Gel"
        SRC_REF: "${{ github.sha }}"
        PKG_REVISION: "<current-date>"
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "debian"
        PKG_PLATFORM_VERSION: "bullseye"
        EXTRA_OPTIMIZATIONS: "true"
        METAPKG_GIT_CACHE: disabled
        GEL_PKG_REF: ${{ inputs.gelpkg_ref }}
        METAPKG_REF: ${{ inputs.metapkg_ref }}

    - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
      with:
        name: builds-debian-bullseye-x86_64
        path: artifacts/debian-bullseye

  build-debian-bullseye-aarch64:
    runs-on: ['package-builder', 'self-hosted', 'linux', 'arm64']
    needs: prep

    if: needs.prep.outputs.if_debian_bullseye_aarch64 == 'true'


    steps:
    - name: Build
      uses: docker://ghcr.io/geldata/gelpkg-build-debian-bullseye:latest@sha256:c9013ed1c6a5347b4c20f240fcaaf3834e88949e46d05f40f496b879f82f8f95
      env:
        PACKAGE: "edgedbpkg.edgedb:Gel"
        SRC_REF: "${{ github.sha }}"
        PKG_REVISION: "<current-date>"
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "debian"
        PKG_PLATFORM_VERSION: "bullseye"
        EXTRA_OPTIMIZATIONS: "true"
        METAPKG_GIT_CACHE: disabled
        GEL_PKG_REF: ${{ inputs.gelpkg_ref }}
        METAPKG_REF: ${{ inputs.metapkg_ref }}

    - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
      with:
        name: builds-debian-bullseye-aarch64
        path: artifacts/debian-bullseye

  build-debian-bookworm-x86_64:
    runs-on: ['package-builder', 'self-hosted', 'linux', 'x64']
    needs: prep

    if: needs.prep.outputs.if_debian_bookworm_x86_64 == 'true'


    steps:
    - name: Build
      uses: docker://ghcr.io/geldata/gelpkg-build-debian-bookworm:latest@sha256:1aa458c2c451fd269d96dbdf1569284f2e1aab790a32a4588472446631e9613f
      env:
        PACKAGE: "edgedbpkg.edgedb:Gel"
        SRC_REF: "${{ github.sha }}"
        PKG_REVISION: "<current-date>"
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "debian"
        PKG_PLATFORM_VERSION: "bookworm"
        EXTRA_OPTIMIZATIONS: "true"
        METAPKG_GIT_CACHE: disabled
        GEL_PKG_REF: ${{ inputs.gelpkg_ref }}
        METAPKG_REF: ${{ inputs.metapkg_ref }}

    - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
      with:
        name: builds-debian-bookworm-x86_64
        path: artifacts/debian-bookworm

  build-debian-bookworm-aarch64:
    runs-on: ['package-builder', 'self-hosted', 'linux', 'arm64']
    needs: prep

    if: needs.prep.outputs.if_debian_bookworm_aarch64 == 'true'


    steps:
    - name: Build
      uses: docker://ghcr.io/geldata/gelpkg-build-debian-bookworm:latest@sha256:1aa458c2c451fd269d96dbdf1569284f2e1aab790a32a4588472446631e9613f
      env:
        PACKAGE: "edgedbpkg.edgedb:Gel"
        SRC_REF: "${{ github.sha }}"
        PKG_REVISION: "<current-date>"
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "debian"
        PKG_PLATFORM_VERSION: "bookworm"
        EXTRA_OPTIMIZATIONS: "true"
        METAPKG_GIT_CACHE: disabled
        GEL_PKG_REF: ${{ inputs.gelpkg_ref }}
        METAPKG_REF: ${{ inputs.metapkg_ref }}

    - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
      with:
        name: builds-debian-bookworm-aarch64
        path: artifacts/debian-bookworm

  build-ubuntu-focal-x86_64:
    runs-on: ['package-builder', 'self-hosted', 'linux', 'x64']
    needs: prep

    if: needs.prep.outputs.if_ubuntu_focal_x86_64 == 'true'


    steps:
    - name: Build
      uses: docker://ghcr.io/geldata/gelpkg-build-ubuntu-focal:latest@sha256:cca585344b61054928d91a5234b219e98d63146448593385eb957da60583a440
      env:
        PACKAGE: "edgedbpkg.edgedb:Gel"
        SRC_REF: "${{ github.sha }}"
        PKG_REVISION: "<current-date>"
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "ubuntu"
        PKG_PLATFORM_VERSION: "focal"
        EXTRA_OPTIMIZATIONS: "true"
        METAPKG_GIT_CACHE: disabled
        GEL_PKG_REF: ${{ inputs.gelpkg_ref }}
        METAPKG_REF: ${{ inputs.metapkg_ref }}

    - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
      with:
        name: builds-ubuntu-focal-x86_64
        path: artifacts/ubuntu-focal

  build-ubuntu-focal-aarch64:
    runs-on: ['package-builder', 'self-hosted', 'linux', 'arm64']
    needs: prep

    if: needs.prep.outputs.if_ubuntu_focal_aarch64 == 'true'


    steps:
    - name: Build
      uses: docker://ghcr.io/geldata/gelpkg-build-ubuntu-focal:latest@sha256:cca585344b61054928d91a5234b219e98d63146448593385eb957da60583a440
      env:
        PACKAGE: "edgedbpkg.edgedb:Gel"
        SRC_REF: "${{ github.sha }}"
        PKG_REVISION: "<current-date>"
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "ubuntu"
        PKG_PLATFORM_VERSION: "focal"
        EXTRA_OPTIMIZATIONS: "true"
        METAPKG_GIT_CACHE: disabled
        GEL_PKG_REF: ${{ inputs.gelpkg_ref }}
        METAPKG_REF: ${{ inputs.metapkg_ref }}

    - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
      with:
        name: builds-ubuntu-focal-aarch64
        path: artifacts/ubuntu-focal

  build-ubuntu-jammy-x86_64:
    runs-on: ['package-builder', 'self-hosted', 'linux', 'x64']
    needs: prep

    if: needs.prep.outputs.if_ubuntu_jammy_x86_64 == 'true'


    steps:
    - name: Build
      uses: docker://ghcr.io/geldata/gelpkg-build-ubuntu-jammy:latest@sha256:13c2cbba72d4adbe4be29155713c3d54208ee638e2b72a5ff231d183fedf9eb5
      env:
        PACKAGE: "edgedbpkg.edgedb:Gel"
        SRC_REF: "${{ github.sha }}"
        PKG_REVISION: "<current-date>"
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "ubuntu"
        PKG_PLATFORM_VERSION: "jammy"
        EXTRA_OPTIMIZATIONS: "true"
        METAPKG_GIT_CACHE: disabled
        GEL_PKG_REF: ${{ inputs.gelpkg_ref }}
        METAPKG_REF: ${{ inputs.metapkg_ref }}

    - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
      with:
        name: builds-ubuntu-jammy-x86_64
        path: artifacts/ubuntu-jammy

  build-ubuntu-jammy-aarch64:
    runs-on: ['package-builder', 'self-hosted', 'linux', 'arm64']
    needs: prep

    if: needs.prep.outputs.if_ubuntu_jammy_aarch64 == 'true'


    steps:
    - name: Build
      uses: docker://ghcr.io/geldata/gelpkg-build-ubuntu-jammy:latest@sha256:13c2cbba72d4adbe4be29155713c3d54208ee638e2b72a5ff231d183fedf9eb5
      env:
        PACKAGE: "edgedbpkg.edgedb:Gel"
        SRC_REF: "${{ github.sha }}"
        PKG_REVISION: "<current-date>"
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "ubuntu"
        PKG_PLATFORM_VERSION: "jammy"
        EXTRA_OPTIMIZATIONS: "true"
        METAPKG_GIT_CACHE: disabled
        GEL_PKG_REF: ${{ inputs.gelpkg_ref }}
        METAPKG_REF: ${{ inputs.metapkg_ref }}

    - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
      with:
        name: builds-ubuntu-jammy-aarch64
        path: artifacts/ubuntu-jammy

  build-ubuntu-noble-x86_64:
    runs-on: ['package-builder', 'self-hosted', 'linux', 'x64']
    needs: prep

    if: needs.prep.outputs.if_ubuntu_noble_x86_64 == 'true'


    steps:
    - name: Build
      uses: docker://ghcr.io/geldata/gelpkg-build-ubuntu-noble:latest@sha256:c9b655a4f52cd1e72c1368e9133e0e656acd2f17b98aea48cfad9fcd1d401e2e
      env:
        PACKAGE: "edgedbpkg.edgedb:Gel"
        SRC_REF: "${{ github.sha }}"
        PKG_REVISION: "<current-date>"
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "ubuntu"
        PKG_PLATFORM_VERSION: "noble"
        EXTRA_OPTIMIZATIONS: "true"
        METAPKG_GIT_CACHE: disabled
        GEL_PKG_REF: ${{ inputs.gelpkg_ref }}
        METAPKG_REF: ${{ inputs.metapkg_ref }}

    - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
      with:
        name: builds-ubuntu-noble-x86_64
        path: artifacts/ubuntu-noble

  build-ubuntu-noble-aarch64:
    runs-on: ['package-builder', 'self-hosted', 'linux', 'arm64']
    needs: prep

    if: needs.prep.outputs.if_ubuntu_noble_aarch64 == 'true'


    steps:
    - name: Build
      uses: docker://ghcr.io/geldata/gelpkg-build-ubuntu-noble:latest@sha256:c9b655a4f52cd1e72c1368e9133e0e656acd2f17b98aea48cfad9fcd1d401e2e
      env:
        PACKAGE: "edgedbpkg.edgedb:Gel"
        SRC_REF: "${{ github.sha }}"
        PKG_REVISION: "<current-date>"
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "ubuntu"
        PKG_PLATFORM_VERSION: "noble"
        EXTRA_OPTIMIZATIONS: "true"
        METAPKG_GIT_CACHE: disabled
        GEL_PKG_REF: ${{ inputs.gelpkg_ref }}
        METAPKG_REF: ${{ inputs.metapkg_ref }}

    - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
      with:
        name: builds-ubuntu-noble-aarch64
        path: artifacts/ubuntu-noble

  build-centos-8-x86_64:
    runs-on: ['package-builder', 'self-hosted', 'linux', 'x64']
    needs: prep

    if: needs.prep.outputs.if_centos_8_x86_64 == 'true'


    steps:
    - name: Build
      uses: docker://ghcr.io/geldata/gelpkg-build-centos-8:latest@sha256:8efd46203664cc2b8b5b0b59c3102aeeb12fff6fcaae759463f0da5a80b1324f
      env:
        PACKAGE: "edgedbpkg.edgedb:Gel"
        SRC_REF: "${{ github.sha }}"
        PKG_REVISION: "<current-date>"
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "centos"
        PKG_PLATFORM_VERSION: "8"
        EXTRA_OPTIMIZATIONS: "true"
        METAPKG_GIT_CACHE: disabled
        GEL_PKG_REF: ${{ inputs.gelpkg_ref }}
        METAPKG_REF: ${{ inputs.metapkg_ref }}

    - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
      with:
        name: builds-centos-8-x86_64
        path: artifacts/centos-8

  build-centos-8-aarch64:
    runs-on: ['package-builder', 'self-hosted', 'linux', 'arm64']
    needs: prep

    if: needs.prep.outputs.if_centos_8_aarch64 == 'true'


    steps:
    - name: Build
      uses: docker://ghcr.io/geldata/gelpkg-build-centos-8:latest@sha256:8efd46203664cc2b8b5b0b59c3102aeeb12fff6fcaae759463f0da5a80b1324f
      env:
        PACKAGE: "edgedbpkg.edgedb:Gel"
        SRC_REF: "${{ github.sha }}"
        PKG_REVISION: "<current-date>"
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "centos"
        PKG_PLATFORM_VERSION: "8"
        EXTRA_OPTIMIZATIONS: "true"
        METAPKG_GIT_CACHE: disabled
        GEL_PKG_REF: ${{ inputs.gelpkg_ref }}
        METAPKG_REF: ${{ inputs.metapkg_ref }}

    - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
      with:
        name: builds-centos-8-aarch64
        path: artifacts/centos-8

  build-rockylinux-9-x86_64:
    runs-on: ['package-builder', 'self-hosted', 'linux', 'x64']
    needs: prep

    if: needs.prep.outputs.if_rockylinux_9_x86_64 == 'true'


    steps:
    - name: Build
      uses: docker://ghcr.io/geldata/gelpkg-build-rockylinux-9:latest@sha256:c231d26f9614eb5a786f25e80cbb16d658170eb59daedced2ca2cf3901bf412e
      env:
        PACKAGE: "edgedbpkg.edgedb:Gel"
        SRC_REF: "${{ github.sha }}"
        PKG_REVISION: "<current-date>"
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "rockylinux"
        PKG_PLATFORM_VERSION: "9"
        EXTRA_OPTIMIZATIONS: "true"
        METAPKG_GIT_CACHE: disabled
        GEL_PKG_REF: ${{ inputs.gelpkg_ref }}
        METAPKG_REF: ${{ inputs.metapkg_ref }}

    - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
      with:
        name: builds-rockylinux-9-x86_64
        path: artifacts/rockylinux-9

  build-rockylinux-9-aarch64:
    runs-on: ['package-builder', 'self-hosted', 'linux', 'arm64']
    needs: prep

    if: needs.prep.outputs.if_rockylinux_9_aarch64 == 'true'


    steps:
    - name: Build
      uses: docker://ghcr.io/geldata/gelpkg-build-rockylinux-9:latest@sha256:c231d26f9614eb5a786f25e80cbb16d658170eb59daedced2ca2cf3901bf412e
      env:
        PACKAGE: "edgedbpkg.edgedb:Gel"
        SRC_REF: "${{ github.sha }}"
        PKG_REVISION: "<current-date>"
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "rockylinux"
        PKG_PLATFORM_VERSION: "9"
        EXTRA_OPTIMIZATIONS: "true"
        METAPKG_GIT_CACHE: disabled
        GEL_PKG_REF: ${{ inputs.gelpkg_ref }}
        METAPKG_REF: ${{ inputs.metapkg_ref }}

    - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
      with:
        name: builds-rockylinux-9-aarch64
        path: artifacts/rockylinux-9

  build-linux-x86_64:
    runs-on: ['package-builder', 'self-hosted', 'linux', 'x64']
    needs: prep

    if: needs.prep.outputs.if_linux_x86_64 == 'true'


    steps:
    - name: Build
      uses: docker://ghcr.io/geldata/gelpkg-build-linux-x86_64:latest@sha256:b3a5b7013547a85c214105e217b75194a5239f9fc2a5c9b49d5cf20a61d30dbb
      env:
        PACKAGE: "edgedbpkg.edgedb:Gel"
        SRC_REF: "${{ github.sha }}"
        PKG_REVISION: "<current-date>"
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "linux"
        PKG_PLATFORM_VERSION: "x86_64"
        EXTRA_OPTIMIZATIONS: "true"
        BUILD_GENERIC: true
        METAPKG_GIT_CACHE: disabled
        GEL_PKG_REF: ${{ inputs.gelpkg_ref }}
        METAPKG_REF: ${{ inputs.metapkg_ref }}

    - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
      with:
        name: builds-linux-x86_64
        path: artifacts/linux-x86_64

  build-linux-aarch64:
    runs-on: ['package-builder', 'self-hosted', 'linux', 'arm64']
    needs: prep

    if: needs.prep.outputs.if_linux_aarch64 == 'true'


    steps:
    - name: Build
      uses: docker://ghcr.io/geldata/gelpkg-build-linux-aarch64:latest@sha256:5207897fc775b1497f64313c840cbcf9c67bc089944d653147e402ddec9630bf
      env:
        PACKAGE: "edgedbpkg.edgedb:Gel"
        SRC_REF: "${{ github.sha }}"
        PKG_REVISION: "<current-date>"
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "linux"
        PKG_PLATFORM_VERSION: "aarch64"
        EXTRA_OPTIMIZATIONS: "true"
        BUILD_GENERIC: true
        METAPKG_GIT_CACHE: disabled
        GEL_PKG_REF: ${{ inputs.gelpkg_ref }}
        METAPKG_REF: ${{ inputs.metapkg_ref }}

    - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
      with:
        name: builds-linux-aarch64
        path: artifacts/linux-aarch64

  build-linuxmusl-x86_64:
    runs-on: ['package-builder', 'self-hosted', 'linux', 'x64']
    needs: prep

    if: needs.prep.outputs.if_linuxmusl_x86_64 == 'true'


    steps:
    - name: Build
      uses: docker://ghcr.io/geldata/gelpkg-build-linuxmusl-x86_64:latest@sha256:154c8293f65a80f89869f107a4fb02e3ec6acdcbfafe106e1fd68c1a094f11de
      env:
        PACKAGE: "edgedbpkg.edgedb:Gel"
        SRC_REF: "${{ github.sha }}"
        PKG_REVISION: "<current-date>"
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "linux"
        PKG_PLATFORM_VERSION: "x86_64"
        EXTRA_OPTIMIZATIONS: "true"
        BUILD_GENERIC: true
        PKG_PLATFORM_LIBC: "musl"
        METAPKG_GIT_CACHE: disabled
        GEL_PKG_REF: ${{ inputs.gelpkg_ref }}
        METAPKG_REF: ${{ inputs.metapkg_ref }}

    - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
      with:
        name: builds-linuxmusl-x86_64
        path: artifacts/linuxmusl-x86_64

  build-linuxmusl-aarch64:
    runs-on: ['package-builder', 'self-hosted', 'linux', 'arm64']
    needs: prep

    if: needs.prep.outputs.if_linuxmusl_aarch64 == 'true'


    steps:
    - name: Build
      uses: docker://ghcr.io/geldata/gelpkg-build-linuxmusl-aarch64:latest@sha256:e51c055764a0552fd533792cb90a6ecf16cf84bfbf60424cfdd48cabd26360ba
      env:
        PACKAGE: "edgedbpkg.edgedb:Gel"
        SRC_REF: "${{ github.sha }}"
        PKG_REVISION: "<current-date>"
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "linux"
        PKG_PLATFORM_VERSION: "aarch64"
        EXTRA_OPTIMIZATIONS: "true"
        BUILD_GENERIC: true
        PKG_PLATFORM_LIBC: "musl"
        METAPKG_GIT_CACHE: disabled
        GEL_PKG_REF: ${{ inputs.gelpkg_ref }}
        METAPKG_REF: ${{ inputs.metapkg_ref }}

    - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
      with:
        name: builds-linuxmusl-aarch64
        path: artifacts/linuxmusl-aarch64

  build-macos-x86_64:
    runs-on: ['macos-13']
    needs: prep

    if: needs.prep.outputs.if_macos_x86_64 == 'true'


    steps:
    - name: Update Homebrew before installing Rust toolchain
      run: |
        # Homebrew renamed `rustup-init` to `rustup`:
        #   https://github.com/Homebrew/homebrew-core/pull/177840
        # But the GitHub Action runner is not updated with this change yet.
        # This caused the later `brew update` in step `Build` to relink Rust
        # toolchain executables, overwriting the custom toolchain installed by
        # `dsherret/rust-toolchain-file`. So let's just run `brew update` early.
        brew update

    - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
      if: true
      with:
        sparse-checkout: |
          rust-toolchain.toml
        sparse-checkout-cone-mode: false

    - name: Install Rust toolchain
      uses: dsherret/rust-toolchain-file@3551321aa44dd44a0393eb3b6bdfbc5d25ecf621  # v1
      if: true

    - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
      with:
        repository: edgedb/edgedb-pkg
        ref: master
        path: edgedb-pkg

    - name: Set up Python
      uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
      if: true
      with:
        python-version: "3.12"

    - name: Set up NodeJS
      uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020  # v7.0.0
      if: true
      with:
        node-version: '20'

    - name: Install dependencies
      if: true
      run: |
        env HOMEBREW_NO_AUTO_UPDATE=1 brew install libmagic

    - name: Install an alias
      # This is probably not strictly needed, but sentencepiece build script reports
      # errors without it.
      if: true
      run: |
        printf '#!/bin/sh\n\nexec sysctl -n hw.logicalcpu' > /usr/local/bin/nproc
        chmod +x /usr/local/bin/nproc

    - name: Build
      env:
        PACKAGE: "edgedbpkg.edgedb:Gel"
        SRC_REF: "${{ github.sha }}"
        PKG_REVISION: "<current-date>"
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "macos"
        PKG_PLATFORM_VERSION: "x86_64"
        PKG_PLATFORM_ARCH: "x86_64"
        EXTRA_OPTIMIZATIONS: "true"
        METAPKG_GIT_CACHE: disabled
        BUILD_GENERIC: true
        CMAKE_POLICY_VERSION_MINIMUM: '3.5'
        GEL_PKG_REF: ${{ inputs.gelpkg_ref }}
        METAPKG_REF: ${{ inputs.metapkg_ref }}
      run: |
        edgedb-pkg/integration/macos/build.sh

    - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
      with:
        name: builds-macos-x86_64
        path: artifacts/macos-x86_64

  build-macos-aarch64:
    runs-on: ['macos-14']
    needs: prep

    if: needs.prep.outputs.if_macos_aarch64 == 'true'


    steps:
    - name: Update Homebrew before installing Rust toolchain
      run: |
        # Homebrew renamed `rustup-init` to `rustup`:
        #   https://github.com/Homebrew/homebrew-core/pull/177840
        # But the GitHub Action runner is not updated with this change yet.
        # This caused the later `brew update` in step `Build` to relink Rust
        # toolchain executables, overwriting the custom toolchain installed by
        # `dsherret/rust-toolchain-file`. So let's just run `brew update` early.
        brew update

    - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
      if: true
      with:
        sparse-checkout: |
          rust-toolchain.toml
        sparse-checkout-cone-mode: false

    - name: Install Rust toolchain
      uses: dsherret/rust-toolchain-file@3551321aa44dd44a0393eb3b6bdfbc5d25ecf621  # v1
      if: true

    - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
      with:
        repository: edgedb/edgedb-pkg
        ref: master
        path: edgedb-pkg

    - name: Set up Python
      uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
      if: true
      with:
        python-version: "3.12"

    - name: Set up NodeJS
      uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020  # v7.0.0
      if: true
      with:
        node-version: '20'

    - name: Install dependencies
      if: true
      run: |
        env HOMEBREW_NO_AUTO_UPDATE=1 brew install libmagic

    - name: Install an alias
      # This is probably not strictly needed, but sentencepiece build script reports
      # errors without it.
      if: true
      run: |
        printf '#!/bin/sh\n\nexec sysctl -n hw.logicalcpu' > /usr/local/bin/nproc
        chmod +x /usr/local/bin/nproc

    - name: Build
      env:
        PACKAGE: "edgedbpkg.edgedb:Gel"
        SRC_REF: "${{ github.sha }}"
        PKG_REVISION: "<current-date>"
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "macos"
        PKG_PLATFORM_VERSION: "aarch64"
        PKG_PLATFORM_ARCH: "aarch64"
        EXTRA_OPTIMIZATIONS: "true"
        METAPKG_GIT_CACHE: disabled
        BUILD_GENERIC: true
        CMAKE_POLICY_VERSION_MINIMUM: '3.5'
        GEL_PKG_REF: ${{ inputs.gelpkg_ref }}
        METAPKG_REF: ${{ inputs.metapkg_ref }}
      run: |
        edgedb-pkg/integration/macos/build.sh

    - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
      with:
        name: builds-macos-aarch64
        path: artifacts/macos-aarch64

  test-debian-buster-x86_64:
    needs: [build-debian-buster-x86_64]
    runs-on: ['package-builder', 'self-hosted', 'linux', 'x64']

    steps:
    - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
      with:
        name: builds-debian-buster-x86_64
        path: artifacts/debian-buster

    - name: Test
      uses: docker://ghcr.io/geldata/gelpkg-test-debian-buster:latest@sha256:101041f2738f5c50d17becc99d1a65cc6fb49247e00fe7333500389bcdbd7847
      env:
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "debian"
        PKG_PLATFORM_VERSION: "buster"
        PKG_PLATFORM_LIBC: ""
        PKG_TEST_SELECT: ""
        PKG_TEST_EXCLUDE: ""
        PKG_TEST_FILES: " "
        # edb test with -j higher than 1 seems to result in workflow
        # jobs getting killed arbitrarily by Github.
        PKG_TEST_JOBS: 0

  test-debian-buster-aarch64:
    needs: [build-debian-buster-aarch64]
    runs-on: ['package-builder', 'self-hosted', 'linux', 'arm64']

    steps:
    - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
      with:
        name: builds-debian-buster-aarch64
        path: artifacts/debian-buster

    - name: Test
      uses: docker://ghcr.io/geldata/gelpkg-test-debian-buster:latest@sha256:101041f2738f5c50d17becc99d1a65cc6fb49247e00fe7333500389bcdbd7847
      env:
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "debian"
        PKG_PLATFORM_VERSION: "buster"
        PKG_PLATFORM_LIBC: ""
        PKG_TEST_SELECT: ""
        PKG_TEST_EXCLUDE: ""
        PKG_TEST_FILES: " "
        # edb test with -j higher than 1 seems to result in workflow
        # jobs getting killed arbitrarily by Github.
        PKG_TEST_JOBS: 0

  test-debian-bullseye-x86_64:
    needs: [build-debian-bullseye-x86_64]
    runs-on: ['package-builder', 'self-hosted', 'linux', 'x64']

    steps:
    - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
      with:
        name: builds-debian-bullseye-x86_64
        path: artifacts/debian-bullseye

    - name: Test
      uses: docker://ghcr.io/geldata/gelpkg-test-debian-bullseye:latest@sha256:27d2eec67a8c37e3b0cbaf19747e094a52aa3b51fffa4d1ebc27632ec9086da3
      env:
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "debian"
        PKG_PLATFORM_VERSION: "bullseye"
        PKG_PLATFORM_LIBC: ""
        PKG_TEST_SELECT: ""
        PKG_TEST_EXCLUDE: ""
        PKG_TEST_FILES: " "
        # edb test with -j higher than 1 seems to result in workflow
        # jobs getting killed arbitrarily by Github.
        PKG_TEST_JOBS: 0

  test-debian-bullseye-aarch64:
    needs: [build-debian-bullseye-aarch64]
    runs-on: ['package-builder', 'self-hosted', 'linux', 'arm64']

    steps:
    - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
      with:
        name: builds-debian-bullseye-aarch64
        path: artifacts/debian-bullseye

    - name: Test
      uses: docker://ghcr.io/geldata/gelpkg-test-debian-bullseye:latest@sha256:27d2eec67a8c37e3b0cbaf19747e094a52aa3b51fffa4d1ebc27632ec9086da3
      env:
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "debian"
        PKG_PLATFORM_VERSION: "bullseye"
        PKG_PLATFORM_LIBC: ""
        PKG_TEST_SELECT: ""
        PKG_TEST_EXCLUDE: ""
        PKG_TEST_FILES: " "
        # edb test with -j higher than 1 seems to result in workflow
        # jobs getting killed arbitrarily by Github.
        PKG_TEST_JOBS: 0

  test-debian-bookworm-x86_64:
    needs: [build-debian-bookworm-x86_64]
    runs-on: ['package-builder', 'self-hosted', 'linux', 'x64']

    steps:
    - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
      with:
        name: builds-debian-bookworm-x86_64
        path: artifacts/debian-bookworm

    - name: Test
      uses: docker://ghcr.io/geldata/gelpkg-test-debian-bookworm:latest@sha256:a516efc1c210f5d48cd98dede35add93ab148c391fcac0fffb55b60f23e24b78
      env:
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "debian"
        PKG_PLATFORM_VERSION: "bookworm"
        PKG_PLATFORM_LIBC: ""
        PKG_TEST_SELECT: ""
        PKG_TEST_EXCLUDE: ""
        PKG_TEST_FILES: " "
        # edb test with -j higher than 1 seems to result in workflow
        # jobs getting killed arbitrarily by Github.
        PKG_TEST_JOBS: 0

  test-debian-bookworm-aarch64:
    needs: [build-debian-bookworm-aarch64]
    runs-on: ['package-builder', 'self-hosted', 'linux', 'arm64']

    steps:
    - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
      with:
        name: builds-debian-bookworm-aarch64
        path: artifacts/debian-bookworm

    - name: Test
      uses: docker://ghcr.io/geldata/gelpkg-test-debian-bookworm:latest@sha256:a516efc1c210f5d48cd98dede35add93ab148c391fcac0fffb55b60f23e24b78
      env:
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "debian"
        PKG_PLATFORM_VERSION: "bookworm"
        PKG_PLATFORM_LIBC: ""
        PKG_TEST_SELECT: ""
        PKG_TEST_EXCLUDE: ""
        PKG_TEST_FILES: " "
        # edb test with -j higher than 1 seems to result in workflow
        # jobs getting killed arbitrarily by Github.
        PKG_TEST_JOBS: 0

  test-ubuntu-focal-x86_64:
    needs: [build-ubuntu-focal-x86_64]
    runs-on: ['package-builder', 'self-hosted', 'linux', 'x64']

    steps:
    - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
      with:
        name: builds-ubuntu-focal-x86_64
        path: artifacts/ubuntu-focal

    - name: Test
      uses: docker://ghcr.io/geldata/gelpkg-test-ubuntu-focal:latest@sha256:990c5aa7ac8dadcd231eea40410ef2151f33b61dbafb9659270f062d7de0454e
      env:
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "ubuntu"
        PKG_PLATFORM_VERSION: "focal"
        PKG_PLATFORM_LIBC: ""
        PKG_TEST_SELECT: ""
        PKG_TEST_EXCLUDE: ""
        PKG_TEST_FILES: " "
        # edb test with -j higher than 1 seems to result in workflow
        # jobs getting killed arbitrarily by Github.
        PKG_TEST_JOBS: 0

  test-ubuntu-focal-aarch64:
    needs: [build-ubuntu-focal-aarch64]
    runs-on: ['package-builder', 'self-hosted', 'linux', 'arm64']

    steps:
    - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
      with:
        name: builds-ubuntu-focal-aarch64
        path: artifacts/ubuntu-focal

    - name: Test
      uses: docker://ghcr.io/geldata/gelpkg-test-ubuntu-focal:latest@sha256:990c5aa7ac8dadcd231eea40410ef2151f33b61dbafb9659270f062d7de0454e
      env:
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "ubuntu"
        PKG_PLATFORM_VERSION: "focal"
        PKG_PLATFORM_LIBC: ""
        PKG_TEST_SELECT: ""
        PKG_TEST_EXCLUDE: ""
        PKG_TEST_FILES: " "
        # edb test with -j higher than 1 seems to result in workflow
        # jobs getting killed arbitrarily by Github.
        PKG_TEST_JOBS: 0

  test-ubuntu-jammy-x86_64:
    needs: [build-ubuntu-jammy-x86_64]
    runs-on: ['package-builder', 'self-hosted', 'linux', 'x64']

    steps:
    - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
      with:
        name: builds-ubuntu-jammy-x86_64
        path: artifacts/ubuntu-jammy

    - name: Test
      uses: docker://ghcr.io/geldata/gelpkg-test-ubuntu-jammy:latest@sha256:bf1d133f28ca7b2126aa833ca419f57e5be79599b6f557c2dcb802b63bab700e
      env:
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "ubuntu"
        PKG_PLATFORM_VERSION: "jammy"
        PKG_PLATFORM_LIBC: ""
        PKG_TEST_SELECT: ""
        PKG_TEST_EXCLUDE: ""
        PKG_TEST_FILES: " "
        # edb test with -j higher than 1 seems to result in workflow
        # jobs getting killed arbitrarily by Github.
        PKG_TEST_JOBS: 0

  test-ubuntu-jammy-aarch64:
    needs: [build-ubuntu-jammy-aarch64]
    runs-on: ['package-builder', 'self-hosted', 'linux', 'arm64']

    steps:
    - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
      with:
        name: builds-ubuntu-jammy-aarch64
        path: artifacts/ubuntu-jammy

    - name: Test
      uses: docker://ghcr.io/geldata/gelpkg-test-ubuntu-jammy:latest@sha256:bf1d133f28ca7b2126aa833ca419f57e5be79599b6f557c2dcb802b63bab700e
      env:
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "ubuntu"
        PKG_PLATFORM_VERSION: "jammy"
        PKG_PLATFORM_LIBC: ""
        PKG_TEST_SELECT: ""
        PKG_TEST_EXCLUDE: ""
        PKG_TEST_FILES: " "
        # edb test with -j higher than 1 seems to result in workflow
        # jobs getting killed arbitrarily by Github.
        PKG_TEST_JOBS: 0

  test-ubuntu-noble-x86_64:
    needs: [build-ubuntu-noble-x86_64]
    runs-on: ['package-builder', 'self-hosted', 'linux', 'x64']

    steps:
    - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
      with:
        name: builds-ubuntu-noble-x86_64
        path: artifacts/ubuntu-noble

    - name: Test
      uses: docker://ghcr.io/geldata/gelpkg-test-ubuntu-noble:latest@sha256:4c5aef7fe3d49fabb72663bf9421a1cc05c1942ae3ac4687d938bb816d21a5d7
      env:
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "ubuntu"
        PKG_PLATFORM_VERSION: "noble"
        PKG_PLATFORM_LIBC: ""
        PKG_TEST_SELECT: ""
        PKG_TEST_EXCLUDE: ""
        PKG_TEST_FILES: " "
        # edb test with -j higher than 1 seems to result in workflow
        # jobs getting killed arbitrarily by Github.
        PKG_TEST_JOBS: 0

  test-ubuntu-noble-aarch64:
    needs: [build-ubuntu-noble-aarch64]
    runs-on: ['package-builder', 'self-hosted', 'linux', 'arm64']

    steps:
    - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
      with:
        name: builds-ubuntu-noble-aarch64
        path: artifacts/ubuntu-noble

    - name: Test
      uses: docker://ghcr.io/geldata/gelpkg-test-ubuntu-noble:latest@sha256:4c5aef7fe3d49fabb72663bf9421a1cc05c1942ae3ac4687d938bb816d21a5d7
      env:
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "ubuntu"
        PKG_PLATFORM_VERSION: "noble"
        PKG_PLATFORM_LIBC: ""
        PKG_TEST_SELECT: ""
        PKG_TEST_EXCLUDE: ""
        PKG_TEST_FILES: " "
        # edb test with -j higher than 1 seems to result in workflow
        # jobs getting killed arbitrarily by Github.
        PKG_TEST_JOBS: 0

  test-centos-8-x86_64:
    needs: [build-centos-8-x86_64]
    runs-on: ['package-builder', 'self-hosted', 'linux', 'x64']

    steps:
    - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
      with:
        name: builds-centos-8-x86_64
        path: artifacts/centos-8

    - name: Test
      uses: docker://ghcr.io/geldata/gelpkg-test-centos-8:latest@sha256:5e59e6a1a3415c326ef63569d863098d94ee603aedfdffd8096e97b5eb281d23
      env:
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "centos"
        PKG_PLATFORM_VERSION: "8"
        PKG_PLATFORM_LIBC: ""
        PKG_TEST_SELECT: ""
        PKG_TEST_EXCLUDE: ""
        PKG_TEST_FILES: " "
        # edb test with -j higher than 1 seems to result in workflow
        # jobs getting killed arbitrarily by Github.
        PKG_TEST_JOBS: 0

  test-centos-8-aarch64:
    needs: [build-centos-8-aarch64]
    runs-on: ['package-builder', 'self-hosted', 'linux', 'arm64']

    steps:
    - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
      with:
        name: builds-centos-8-aarch64
        path: artifacts/centos-8

    - name: Test
      uses: docker://ghcr.io/geldata/gelpkg-test-centos-8:latest@sha256:5e59e6a1a3415c326ef63569d863098d94ee603aedfdffd8096e97b5eb281d23
      env:
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "centos"
        PKG_PLATFORM_VERSION: "8"
        PKG_PLATFORM_LIBC: ""
        PKG_TEST_SELECT: ""
        PKG_TEST_EXCLUDE: ""
        PKG_TEST_FILES: " "
        # edb test with -j higher than 1 seems to result in workflow
        # jobs getting killed arbitrarily by Github.
        PKG_TEST_JOBS: 0

  test-rockylinux-9-x86_64:
    needs: [build-rockylinux-9-x86_64]
    runs-on: ['package-builder', 'self-hosted', 'linux', 'x64']

    steps:
    - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
      with:
        name: builds-rockylinux-9-x86_64
        path: artifacts/rockylinux-9

    - name: Test
      uses: docker://ghcr.io/geldata/gelpkg-test-rockylinux-9:latest@sha256:42df9236a22c0313f2c36dd1c099df5198a2c1a8c15bf36914656470d1ac63a0
      env:
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "rockylinux"
        PKG_PLATFORM_VERSION: "9"
        PKG_PLATFORM_LIBC: ""
        PKG_TEST_SELECT: ""
        PKG_TEST_EXCLUDE: ""
        PKG_TEST_FILES: " "
        # edb test with -j higher than 1 seems to result in workflow
        # jobs getting killed arbitrarily by Github.
        PKG_TEST_JOBS: 0

  test-rockylinux-9-aarch64:
    needs: [build-rockylinux-9-aarch64]
    runs-on: ['package-builder', 'self-hosted', 'linux', 'arm64']

    steps:
    - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
      with:
        name: builds-rockylinux-9-aarch64
        path: artifacts/rockylinux-9

    - name: Test
      uses: docker://ghcr.io/geldata/gelpkg-test-rockylinux-9:latest@sha256:42df9236a22c0313f2c36dd1c099df5198a2c1a8c15bf36914656470d1ac63a0
      env:
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "rockylinux"
        PKG_PLATFORM_VERSION: "9"
        PKG_PLATFORM_LIBC: ""
        PKG_TEST_SELECT: ""
        PKG_TEST_EXCLUDE: ""
        PKG_TEST_FILES: " "
        # edb test with -j higher than 1 seems to result in workflow
        # jobs getting killed arbitrarily by Github.
        PKG_TEST_JOBS: 0

  test-linux-x86_64:
    needs: [build-linux-x86_64]
    runs-on: ['package-builder', 'self-hosted', 'linux', 'x64']

    steps:
    - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
      with:
        name: builds-linux-x86_64
        path: artifacts/linux-x86_64

    - name: Test
      uses: docker://ghcr.io/geldata/gelpkg-test-linux-x86_64:latest@sha256:6cf55d7e607c4dbef3913a7b52060a3a247c75fae05455c8eb05467883b733e6
      env:
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "linux"
        PKG_PLATFORM_VERSION: "x86_64"
        PKG_PLATFORM_LIBC: ""
        PKG_TEST_SELECT: ""
        PKG_TEST_EXCLUDE: ""
        PKG_TEST_FILES: " "
        # edb test with -j higher than 1 seems to result in workflow
        # jobs getting killed arbitrarily by Github.
        PKG_TEST_JOBS: 0

  test-linux-aarch64:
    needs: [build-linux-aarch64]
    runs-on: ['package-builder', 'self-hosted', 'linux', 'arm64']

    steps:
    - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
      with:
        name: builds-linux-aarch64
        path: artifacts/linux-aarch64

    - name: Test
      uses: docker://ghcr.io/geldata/gelpkg-test-linux-aarch64:latest@sha256:7c1d1e8b9e333f9757d05a41282faa1e22952613197ecf3fad164a4f8c0121c1
      env:
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "linux"
        PKG_PLATFORM_VERSION: "aarch64"
        PKG_PLATFORM_LIBC: ""
        PKG_TEST_SELECT: ""
        PKG_TEST_EXCLUDE: ""
        PKG_TEST_FILES: " "
        # edb test with -j higher than 1 seems to result in workflow
        # jobs getting killed arbitrarily by Github.
        PKG_TEST_JOBS: 0

  test-linuxmusl-x86_64:
    needs: [build-linuxmusl-x86_64]
    runs-on: ['package-builder', 'self-hosted', 'linux', 'x64']

    steps:
    - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
      with:
        name: builds-linuxmusl-x86_64
        path: artifacts/linuxmusl-x86_64

    - name: Test
      uses: docker://ghcr.io/geldata/gelpkg-test-linuxmusl-x86_64:latest@sha256:f5041db317d99f1c6600d29ad3a5fa18e47377516688dc233cc9135d48e389c3
      env:
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "linux"
        PKG_PLATFORM_VERSION: "x86_64"
        PKG_PLATFORM_LIBC: "musl"
        PKG_TEST_SELECT: ""
        PKG_TEST_EXCLUDE: ""
        PKG_TEST_FILES: " "
        # edb test with -j higher than 1 seems to result in workflow
        # jobs getting killed arbitrarily by Github.
        PKG_TEST_JOBS: 0

  test-linuxmusl-aarch64:
    needs: [build-linuxmusl-aarch64]
    runs-on: ['package-builder', 'self-hosted', 'linux', 'arm64']

    steps:
    - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
      with:
        name: builds-linuxmusl-aarch64
        path: artifacts/linuxmusl-aarch64

    - name: Test
      uses: docker://ghcr.io/geldata/gelpkg-test-linuxmusl-aarch64:latest@sha256:1a38306c50ff63bbbbbd662d73cb4e8a68efa0b5c45bdd39989df6d492f7b510
      env:
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "linux"
        PKG_PLATFORM_VERSION: "aarch64"
        PKG_PLATFORM_LIBC: "musl"
        PKG_TEST_SELECT: ""
        PKG_TEST_EXCLUDE: ""
        PKG_TEST_FILES: " "
        # edb test with -j higher than 1 seems to result in workflow
        # jobs getting killed arbitrarily by Github.
        PKG_TEST_JOBS: 0

  test-macos-x86_64:
    needs: [build-macos-x86_64]
    runs-on: ['macos-13']

    steps:
    - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
      with:
        repository: edgedb/edgedb-pkg
        ref: master
        path: edgedb-pkg

    - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
      with:
        name: builds-macos-x86_64
        path: artifacts/macos-x86_64

    - name: Test
      env:
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "macos"
        PKG_PLATFORM_VERSION: "x86_64"
        PKG_TEST_SELECT: ""
        PKG_TEST_EXCLUDE: ""
        PKG_TEST_FILES: " test_dump*.py test_backend_*.py test_database.py test_server_*.py test_edgeql_ddl.py test_session.py
"
      run: |
        # Bump shmmax and shmall to avoid test failures.
        sudo sysctl -w kern.sysv.shmmax=12582912
        sudo sysctl -w kern.sysv.shmall=12582912
        edgedb-pkg/integration/macos/test.sh

  test-macos-aarch64:
    needs: [build-macos-aarch64]
    runs-on: ['macos-14']

    steps:
    - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
      with:
        repository: edgedb/edgedb-pkg
        ref: master
        path: edgedb-pkg

    - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
      with:
        name: builds-macos-aarch64
        path: artifacts/macos-aarch64

    - name: Test
      env:
        PKG_SUBDIST: "nightly"
        PKG_PLATFORM: "macos"
        PKG_PLATFORM_VERSION: "aarch64"
        PKG_TEST_SELECT: ""
        PKG_TEST_EXCLUDE: ""
        PKG_TEST_FILES: " "
      run: |
        edgedb-pkg/integration/macos/test.sh
